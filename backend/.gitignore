.vercel
node_modules/
dist/
logs/
*.log

# Tesseract language models are downloaded at runtime — never commit the blobs.
*.traineddata

# Local env (the template is committed)
.env
.env.*
!.env.example
