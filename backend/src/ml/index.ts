/**
 * NSIE machine-learning module (placeholder).
 *
 * The detection engine in ../threat-engine is fully deterministic today. Learned
 * models (NSIE v3) will attach here without changing that contract: the engine
 * stays the source of truth for scores, and any model output is an additional
 * signal, never the final word. See docs/nsie/ml-architecture.md.
 */

export {};
