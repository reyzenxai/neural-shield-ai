// This package is declaration-only. Every export is a TypeScript type, so it is
// erased at compile time and nothing here runs. This file exists only so module
// resolution has a valid "main" to point at.
module.exports = {};
