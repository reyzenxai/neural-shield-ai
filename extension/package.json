{
  "name": "neural-shield-extension",
  "version": "0.1.0",
  "description": "Neural Shield AI — Chrome extension MV3",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.317",
    "esbuild": "^0.25.5",
    "typescript": "^5.7.2"
  }
}
